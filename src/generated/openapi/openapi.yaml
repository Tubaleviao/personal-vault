# @generated by @newel/generator-openapi — do not edit
openapi: 3.0.3
info:
  title: PersonalVault
  description: "User-owned personal data store with granular, revocable, auditable consent"
  version: 0.1.0

paths:
  /vault/me:
    get:
      summary: Return the vault owner profile
      operationId: getVaultMe
      security:
        - bearerAuth: [owner]
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VaultUser'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
  /vault/lock:
    post:
      summary: Seal the vault and wipe session keys
      operationId: postVaultLock
      responses:
        '200':
          description: Successful response
  /vault/unlock:
    post:
      summary: Unseal the vault by verifying the passphrase
      operationId: postVaultUnlock
      responses:
        '200':
          description: Successful response
  /vault/recover/start:
    post:
      summary: Begin recovery using the BIP-39 phrase
      operationId: postVaultRecoverStart
      responses:
        '200':
          description: Successful response
  /vault/recover/complete:
    post:
      summary: Complete recovery with a new passphrase
      operationId: postVaultRecoverComplete
      responses:
        '200':
          description: Successful response
  /vault/claims:
    get:
      summary: List all claims in the vault
      operationId: getVaultClaims
      security:
        - bearerAuth: [owner]
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Claim'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
    post:
      summary: Add a new claim to the vault
      operationId: postVaultClaims
      security:
        - bearerAuth: [owner]
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Claim'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
  "/vault/claims/{id}":
    get:
      summary: Read one claim
      operationId: getVaultClaimsById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      security:
        - bearerAuth: [owner]
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Claim'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
    delete:
      summary: Permanently delete a claim
      operationId: deleteVaultClaimsById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      security:
        - bearerAuth: [owner]
      responses:
        '204':
          description: No content
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
  /vault/grants:
    get:
      summary: List all grants
      operationId: getVaultGrants
      security:
        - bearerAuth: [owner]
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Grant'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
    post:
      summary: Create a new grant (push or pull)
      operationId: postVaultGrants
      security:
        - bearerAuth: [owner]
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Grant'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
  "/vault/grants/{id}":
    get:
      summary: Read one grant
      operationId: getVaultGrantsById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      security:
        - bearerAuth: [owner]
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Grant'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
  "/vault/grants/{id}/revoke":
    post:
      summary: Revoke a grant immediately
      operationId: postVaultGrantsByIdRevoke
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Successful response
  /vault/audit:
    get:
      summary: "List audit log entries, newest first"
      operationId: getVaultAudit
      security:
        - bearerAuth: [owner]
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditEntry'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
  "/relay/bundles/{grantId}":
    put:
      summary: Upload an encrypted bundle for a pull grant; body is opaque ciphertext
      operationId: putRelayBundlesByGrantId
      parameters:
        - name: grantId
          in: path
          required: true
          schema:
            type: string
      security:
        - bearerAuth: [owner]
      responses:
        '200':
          description: Successful response
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
    get:
      summary: Download an encrypted bundle; relay checks grant is active before serving
      operationId: getRelayBundlesByGrantId
      parameters:
        - name: grantId
          in: path
          required: true
          schema:
            type: string
      security:
        - bearerAuth: [grantee]
      responses:
        '200':
          description: Successful response
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
    delete:
      summary: Remove a bundle from the relay (called automatically on revoke)
      operationId: deleteRelayBundlesByGrantId
      parameters:
        - name: grantId
          in: path
          required: true
          schema:
            type: string
      security:
        - bearerAuth: [owner]
      responses:
        '204':
          description: No content
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
  "/grants/{id}/expire":
    post:
      summary: "System-triggered: marks the grant expired when past its expiresAt or after single-use access"
      operationId: postGrantsByIdExpire
      security:
        - bearerAuth: [system]
      responses:
        '200':
          description: Successful response
        '401':
          description: Unauthorized
        '403':
          description: Forbidden

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    VaultUser:
      type: object
      description: "The vault owner, identified by a self-sovereign DID"
      required:
        - id
        - did
        - status
        - createdAt
      properties:
        id:
          type: string
          format: uuid
        did:
          type: string
          description: "did:key identifier derived from the owner Ed25519 keypair"
        displayName:
          type: string
          nullable: true
          description: Optional human-readable label shown in the UI
        status:
          type: string
          enum:
            - active
            - locked
            - recovering
        createdAt:
          type: string
          format: date-time
    Claim:
      type: object
      description: "A single typed data item stored in the vault, individually addressable for field-level consent"
      required:
        - id
        - ownerId
        - type
        - value
        - source
        - verification
        - issuedAt
      properties:
        id:
          type: string
          format: uuid
        ownerId:
          type: string
          format: uuid
        type:
          type: string
          description: "schema.org or W3C VC type, e.g. \"schema:givenName\", \"schema:birthDate\""
        value:
          type: object
          description: Encrypted claim payload; plaintext never persisted unencrypted
        source:
          type: string
          description: Provenance of this claim
          enum:
            - self-attested
            - issuer-signed
            - imported
        verification:
          type: string
          description: Verification level
          enum:
            - none
            - self
            - verified
        issuedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
          nullable: true
          description: Optional expiry; null means no expiry
        issuerDid:
          type: string
          nullable: true
          description: DID of the external issuer if source=issuer-signed
    Grant:
      type: object
      description: "A signed consent record: which claims are shared with which party, for what purpose, until when"
      required:
        - id
        - ownerId
        - granteeRef
        - claimIds
        - purpose
        - mode
        - singleUse
        - ownerSig
        - status
        - createdAt
      properties:
        id:
          type: string
          format: uuid
        ownerId:
          type: string
          format: uuid
        granteeRef:
          type: string
          description: "Opaque identifier for the recipient (DID, email, app name, URL)"
        claimIds:
          type: object
          description: Ordered list of Claim.id values included in this grant
        purpose:
          type: string
          description: Human-readable statement of why the data is shared
        mode:
          type: string
          description: push = encrypted bundle handed to grantee; pull = relay serves on request
          enum:
            - push
            - pull
        singleUse:
          type: boolean
          description: "If true, grant is automatically revoked after first access"
        expiresAt:
          type: string
          format: date-time
          nullable: true
          description: Hard expiry; null means no expiry for pull grants
        ownerSig:
          type: string
          description: "Ed25519 signature over the canonical grant payload, base64url"
        status:
          type: string
          enum:
            - active
            - revoked
            - expired
        createdAt:
          type: string
          format: date-time
        revokedAt:
          type: string
          format: date-time
          nullable: true
    AuditEntry:
      type: object
      description: "One record in the append-only, hash-chained audit log"
      required:
        - id
        - ownerId
        - action
        - actor
        - entryHash
        - createdAt
      properties:
        id:
          type: string
          format: uuid
        ownerId:
          type: string
          format: uuid
        grantId:
          type: string
          format: uuid
          nullable: true
          description: "Grant involved, if applicable"
        action:
          type: string
          description: The event type
          enum:
            - grant-created
            - grant-revoked
            - grant-expired
            - claim-added
            - claim-deleted
            - vault-unlocked
            - vault-locked
            - recovery-started
            - recovery-completed
            - bundle-accessed
        actor:
          type: string
          description: "Who triggered the event: \"owner\", \"system\", or a grantee DID"
        detail:
          type: object
          nullable: true
          description: "Optional structured context (claim types shared, grantee ref, etc.)"
        prevHash:
          type: string
          nullable: true
          description: SHA-256 hash of the previous AuditEntry; null for the genesis entry
        entryHash:
          type: string
          description: "SHA-256 hash of this entry's canonical payload including prevHash"
        createdAt:
          type: string
          format: date-time
