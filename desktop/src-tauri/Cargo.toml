[package]
name = "personal-vault"
version = "0.1.0"
description = "Personal Data Vault — desktop host"
authors = []
edition = "2021"

[lib]
name = "personal_vault_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "personal-vault"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs-next = "2"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
